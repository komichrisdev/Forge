# Qwen Mini Scaffold: FG-180

- Generated: `2026-08-04T16:51:26.327645+00:00`
- Model: `local-qwen3-14b-debian`

```json
{
  "scaffold": {
    "files": {
      "src/models/work_item.py": "WorkItem ORM model with title, description, status, priority, owner, due_date, labels, sources, updates, execution_run_id, audit_event_id",
      "src/api/work_item_routes.py": "REST endpoints for CRUD + status transitions with permission checks",
      "src/services/work_item_service.py": "Business logic for validation, concurrency control, and linking to execution ledger",
      "migrations/versions/180_work_item_schema.py": "Alembic migration for work_item table with foreign keys to execution runs",
      "tests/test_work_item.py": "Unit tests for model constraints, API endpoints, and concurrency scenarios",
      "frontend/components/WorkItemList.vue": "Paginated list with filters (status, owner, labels, date ranges)",
      "frontend/components/WorkItemDetail.vue": "Editable detail view with activity history and linked artifacts",
      "integrations/jira.py": "Optional Jira sync adapter (read-only import/export)",
      "docs/work_item_system.md": "Schema diagrams, API docs, and usage examples"
    },
    "sequence": [
      "Design schema with foreign keys to execution runs (FG-070/FG-160)",
      "Implement ORM model with optimistic concurrency (versioning/timestamps)",
      "Build API with permission enforcement (RBAC/ABAC)",
      "Create UI components with pagination and filtering",
      "Add migration scripts and rollback safety checks",
      "Validate large dataset performance with 10k+ item benchmarks"
    ],
    "assumptions": [
      "Execution ledger (FG-070) already tracks run/artifact relationships",
      "Database supports efficient joins between work items and execution runs",
      "Frontend can handle 1000+ item lists without UI lag",
      "Jira integration will use existing FG-070 API patterns"
    ],
    "risks": [
      "Linking work items to execution runs may require schema changes in FG-070",
      "Optimistic concurrency may conflict with existing audit event tracking",
      "Large dataset performance may require indexed fields or caching"
    ],
    "handoff": {
      "required": [
        "Implementation diff showing model relationships",
        "Test coverage report for critical paths",
        "Migration guide with rollback steps",
        "UI performance metrics (load time for 10k items)",
        "Jira integration validation (if implemented)"
      ],
      "next_steps": [
        "Add real-time updates via WebSocket (FG-210)",
        "Implement machine learning for priority auto-assignment (FG-230)"
      ]
    }
  }
}
```
