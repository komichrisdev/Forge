# Qwen Mini Scaffold: FG-050

- Generated: `2026-08-04T04:51:44.100157+00:00`
- Model: `local-qwen3-14b-debian`

```json
{
  "scaffold": {
    "id": "FG-050",
    "title": "Unified Logs presentation",
    "files": {
      "backend": [
        "src/api/logs.py",          // Combined read model endpoint
        "src/models/journal.py",    // Task/audit + notification-delivery ORM
        "src/migrations/0024_unified_logs.py" // DB view or materialized view
      ],
      "frontend": [
        "src/components/LogsScreen.jsx", // Chronological list with filters
        "src/services/logsService.js",   // Paginated data fetching
        "src/state/logsSlice.js"        // Filter state management
      ],
      "tests": [
        "tests/api/test_logs.py",       // Combined data endpoint tests
        "tests/frontend/test_logs.jsx"  // Filter/pagination/ui tests
      ],
      "config": [
        "config/navigation.js"         // Move Logs to final nav position
      ]
    },
    "sequence": [
      "Create unified read model with JOIN on task/audit and notification-delivery records",
      "Implement backend endpoint with filters (severity, source, delivery state, time bounds)",
      "Build frontend LogsScreen with chronological rendering and filter controls",
      "Add pagination controls (load more/prev/next)",
      "Update navigation to position Logs last while preserving legacy deep links",
      "Test chronological ordering, filter behavior, and delivery failure visibility"
    ],
    "assumptions": [
      "Existing task/audit and notification-delivery journals are immutable",
      "Database supports efficient JOINs on timestamped records",
      "Frontend framework supports pagination state management"
    ],
    "risks": [
      "Performance degradation with large datasets in combined read model",
      "Legacy deep links may break if URL paths change",
      "Filtering on delivery state may require new backend validation"
    ],
    "handoff": {
      "deliverables": [
        "Implementation diff showing merged read model",
        "Test results for filter/pagination edge cases",
        "Navigation update confirmation",
        "Documentation for new logs endpoint"
      ],
      "review_cycle": {
        "max_attempts": 3,
        "required_signoffs": ["backend", "frontend", "qa"]
      }
    }
  }
}
```
