# Qwen Mini Scaffold: FG-200

- Generated: `2026-08-04T18:38:49.849522+00:00`
- Model: `local-qwen3-14b-debian`

**FG-200 Scaffold: Cross-feature Integration & Audit Handoff**  

---

### **Files/Components**  
- **Test Suites**:  
  - `tests/backend/integration_test.py` (Active Runs, Dispatch, Prompt Assistant)  
  - `tests/frontend/e2e_test.js` (Image queue, collections, gallery, logs)  
  - `tests/security/vulnerability_scan.sh` (Security checks)  
  - `tests/migrations/reversibility_test.py` (Migration rollback validation)  

- **Documentation**:  
  - `docs/architecture.md` (Updated architecture diagrams)  
  - `docs/api_reference.md` (API changes)  
  - `docs/audit_procedure.md` (Audit commands, rollback steps)  

- **Fixtures/Configs**:  
  - `fixtures/end_to_end_fixture.yaml` (Test data for cross-feature flow)  
  - `config/feature_flags.yaml` (Default safe states for flags)  

- **Audit Artifacts**:  
  - `audit/commit_map.json` (Commit series with hashes and authors)  
  - `audit/changed_files.csv` (Inventory of modified files)  
  - `audit/risk_register.md` (Known risks, mitigation steps)  

---

### **Sequence**  
1. **Setup**:  
   - Spin up local backend/frontend with dependencies (FG-080–190).  
   - Ensure no deployments/pushes (safety: `no_push`, `no_deploy`).  

2. **Test Execution**:  
   - Run full test suites (`pytest`, `jest`, security scans).  
   - Compare results against baseline (pre-FG-200).  
   - Execute end-to-end fixture across all Planning-tab features.  

3. **Validation**:  
   - Confirm feature flags default to safe states.  
   - Verify destructive actions (trash/restore) do not mutate data.  
   - Check migrations are reversible (e.g., `alembic downgrade`).  

4. **Documentation Update**:  
   - Sync API/operator docs with code changes.  
   - Add rollback procedures for critical components.  

5. **Audit Package**:  
   - Generate `commit_map.json`, `changed_files.csv`, and risk register.  
   - Document exact CLI commands for Codex audit (e.g., `git log`, `pytest --baseline`).  

---

### **Assumptions**  
- Dependencies (FG-080–190) are fully implemented and tested.  
- Local environment mirrors production schema/permissions.  
- Feature flags are isolated and do not leak to other environments.  

---

### **Risks**  
- **Test Flakiness**: E2E tests may fail due to timing issues in cross-feature flows.  
- **Doc Gaps**: Documentation may lag behind code changes.  
- **Migration Conflicts**: Reversible migrations may conflict with prior schema changes.  
- **Audit Tooling**: Codex may require specific formatting for commit maps/audit logs.  

---

### **Handoff**  
- **Artifacts**:  
  - `audit/fg-200_handoff.tar.gz` (Includes diff, test results, docs, risk register).  
  - CLI commands for Codex:  
    ```bash  
    git log --oneline --graph --decorate > audit/commit_map.txt  
    pytest tests/ --baseline > audit/test_comparison.txt  
    ```  
- **Review**:  
  - Independent reviewer validates acceptance criteria (no regressions, no deployments).  
  - Codex audit confirms reproducibility of all checks.  

--- 

**Status**: Scaffold ready. No code changes committed yet.
