# Qwen Mini Scaffold: FG-090

- Generated: `2026-08-04T08:38:38.276829+00:00`
- Model: `local-qwen3-14b-debian`

**FG-090 Scaffold Implementation**  
*(Auto-committed structure for prompt-assistant service with model-quality controls)*  

---

### **Files Created**  
- `src/prompt_assistant.py`  
  - Core service logic: request routing, model selection, diff generation  
- `models/eligible_models.py`  
  - Registry of approved models (text-only, no image models)  
- `config/model_registry.yaml`  
  - Configurable model eligibility status (enabled/disabled/ignored)  
- `schemas/prompt_schema.py`  
  - Pydantic models for request/response (original_prompt, revised_prompt, diff, metadata)  
- `tests/test_prompt_assistant.py`  
  - Unit tests for routing, diff logic, model exclusion, direct mode fallback  
- `docs/fg090_architecture.md`  
  - Diagram of service flow: user → router → model → feedback loop  

---

### **Key Components**  
1. **PromptAssistantService**  
   - Routes requests to eligible models (via `swarm_router`)  
   - Handles clarifying questions (if `allow_questions` flag is set)  
   - Returns structured diff between original/revised prompts  

2. **EligibleModelRegistry**  
   - Tracks model status (enabled/disabled/ignored)  
   - Enforces global state (no automatic re-enablement of ignored models)  

3. **ModelQualityMonitor**  
   - Logs metrics: model_used, latency, outcome (success/error), lightweight feedback (e.g., "useful" flag)  
   - No private prompt text stored (schema excludes `original_prompt` in logs)  

---

### **Sequence**  
1. User submits text-only prompt → `PromptAssistantService`  
2. Service checks model registry → routes to eligible model (or direct mode if all disabled)  
3. Model returns revised prompt → service generates diff (via `difflib`)  
4. Result includes:  
   - Original/revised prompts  
   - Structured diff (unified format)  
   - Metadata: model name, latency, outcome, feedback flag  
5. If model output is malformed → fallback to direct mode (user’s original prompt)  

---

### **Assumptions**  
- FG-060 provides baseline model routing infrastructure  
- Model registry is pre-populated with eligible models (no image models)  
- Feedback system uses anonymized metrics (no PII or private prompt storage)  
- Direct mode is preserved for malformed outputs (no silent failures)  

---

### **Risks**  
- **Model selection errors**: Incorrect routing to ineligible models (mitigated by registry checks)  
- **Feedback data leakage**: Accidental logging of private prompt text (mitigated by schema design)  
- **Image submission bypass**: User might request image generation (handled by explicit action flag in `swarm_router`)  

---

### **Handoff**  
- **Artifacts**:  
  - Implementation diff (src/prompt_assistant.py)  
  - Test results (test_prompt_assistant.py)  
  - Baseline comparison (pre/post FG-060)  
  - Rollback notes: revert to FG-060 if model-quality controls fail  
- **Review**:  
  - Independent validation of diff logic, model exclusion, and direct mode fallback  
  - Ensure no image models are inadvertently included in eligible pool  

---

**Status**: Scaffold committed. Next: QA validation of diff logic and model routing.
