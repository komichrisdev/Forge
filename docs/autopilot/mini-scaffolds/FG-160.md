# Qwen Mini Scaffold: FG-160

- Generated: `2026-08-04T15:08:30.645361+00:00`
- Model: `local-qwen3-14b-debian`

### FG-160 Scaffold: Night Owl & Recurring Profiles in Dispatch  
*(Concise implementation blueprint for automated review and incremental development)*  

---

#### **Files/Components**  
**Backend (Python/Dispatch):**  
- `models/profile.py`: ORM model for `NightOwlProfile`, `SwarmTaskerProfile` (inherits from `DispatchTask`)  
- `apis/profile_routes.py`: Endpoints for `GET /profiles/{id}`, `POST /profiles/{id}/run`, `POST /profiles/{id}/pause`  
- `services/profile_service.py`: Logic for validating permissions, enforcing Night Owl policies, audit logging  
- `migrations/0016_profiles.py`: Database schema for profile metadata (schedule, intake state, last run)  
- `config/feature_flags.py`: Flag `FEATURE_NIGHTOWL_MANUAL_CONTROLS` (default: `False`)  

**Frontend (React/Dashboard):**  
- `src/components/ProfileCard.jsx`: Displays profile state, next/last run, job status  
- `src/pages/DispatchProfiles.jsx`: List of approved profiles with "Run Now", "Dry Run", "Pause/Resume" buttons  
- `src/hooks/useProfileActions.jsx`: Handles API calls for manual controls (with permission checks)  

**Tests:**  
- `tests/test_profile_service.py`: Unit tests for policy enforcement, audit logging, error cases  
- `tests/integration/test_profile_routes.py`: Integration tests for unauthorized access, dry-run success  
- `frontend/src/__tests__/ProfileCard.test.jsx`: UI tests for button states, error messages  

**Docs:**  
- `docs/nightowl_integration.md`: Policy enforcement rules, manual control limitations  

---

#### **Sequence**  
1. **Model Setup**: Define `NightOwlProfile` with stricter fields (deadline, worktree, approval status).  
2. **API Endpoints**: Add `/profiles/{id}/run` (POST) with payload: `{"type": "dry_run/manual"}`.  
3. **Policy Enforcement**: In `profile_service.py`, block unauthorized actions (e.g., non-approved profiles).  
4. **Frontend Integration**: Render profile cards with controls, disable buttons for unapproved profiles.  
5. **Audit Logging**: Journal all manual runs in `DispatchTask` table with `source="manual"`.  
6. **Testing**: Validate:  
   - Unauthorized users cannot trigger runs.  
   - Dry runs create `status="dry_run"` tasks.  
   - Pause/resume updates `schedule_state` without deleting queued tasks.  

---

#### **Assumptions**  
- Existing `DispatchTask` model supports `status`, `source`, and `audit_log` fields.  
- Night Owl profiles are pre-approved and stored in a separate table (`nightowl_profiles`).  
- SwarmTasker profiles are already validated and linked to `DispatchTask` via foreign key.  
- Frontend has access to `user_permissions` for UI-level control disabling.  

---

#### **Risks**  
- **Policy Bypass**: Manual controls might inadvertently weaken Night Owl’s approval/worktree checks.  
- **Audit Gaps**: Manual runs could fail to log sufficient context (e.g., user ID, timestamp).  
- **UI Confusion**: Users might misinterpret "Pause" as stopping all future runs (clarify with tooltips).  

---

#### **Handoff**  
- **Artifacts**:  
  - `git diff` showing model/API changes.  
  - Test results for unauthorized access, dry-run success, and policy enforcement.  
  - Documentation on how to toggle `FEATURE_NIGHTOWL_MANUAL_CONTROLS`.  
- **Next Steps**:  
  - Reviewer validates audit logs for manual runs.  
  - Frontend team integrates UI controls with backend permissions.  
  - QA tests edge cases (e.g., paused profile resuming after deadline).  

--- 

*This scaffold assumes FG-070 is complete and Dispatch’s core task model is stable.*
