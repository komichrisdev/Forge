# Qwen Mini Scaffold: FG-140

- Generated: `2026-08-04T13:39:04.891160+00:00`
- Model: `local-qwen3-14b-debian`

{
  "scaffold": {
    "files": [
      "models/artifact.py", 
      "controllers/artifact_controller.py", 
      "forms/delete_confirm_form.py", 
      "services/audit_service.py", 
      "migrations/0014_add_trash_state.py",
      "tests/test_artifact_delete.py"
    ],
    "components": {
      "SoftDeleteMixin": "Adds is_deleted/trash_date fields to Artifact model",
      "DeleteConfirmForm": "CSRF-protected form with artifact ID validation",
      "AuditLogService": "Records deletion/restore events with user context",
      "TrashCanService": "Handles state transitions and policy checks"
    },
    "sequence": [
      "User submits delete request with artifact ID",
      "Auth check + policy validation (pinned/shared status)",
      "If allowed: mark is_deleted=True, set trash_date",
      "Log audit event with 'soft_delete' action",
      "Redirect to artifact list with success message",
      "Restore endpoint: validate trash state + policy",
      "If allowed: set is_deleted=False, clear trash_date",
      "Log audit event with 'restore' action"
    ],
    "assumptions": [
      "Existing auth system handles user permissions",
      "Artifact model has 'is_deleted' boolean field",
      "Audit log table exists with action/user_id/artifact_id fields",
      "CSRF protection is enabled in framework config"
    ],
    "risks": [
      "Incomplete policy validation for shared/pinned artifacts",
      "Race conditions in concurrent delete/restore operations",
      "Audit log missing context (e.g., user roles during deletion)"
    ],
    "handoff": {
      "reviewers": ["security_team", "data_integrity_team"],
      "deliverables": [
        "Implementation diff with trash state transitions",
        "Test coverage for 403/404 cases in delete/restore",
        "Audit log sample records for both actions"
      ],
      "next_steps": "Integrate with FG-120's artifact listing filters"
    }
  }
}
