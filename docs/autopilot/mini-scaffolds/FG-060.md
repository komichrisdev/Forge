# Qwen Mini Scaffold: FG-060

- Generated: `2026-08-04T05:49:40.419130+00:00`
- Model: `local-qwen3-14b-debian`

**FG-060 Scaffold: Unified Agents & Models View**  
*(Rough implementation structure for review and iteration)*  

---

### **Files/Components**  
**Frontend (web/frontend):**  
- `components/AgentModelDashboard.jsx`  
  - Unified view for agents/models with tabs:  
    - **Agent Overview** (logical agent identity, role, active run, fallback chain)  
    - **Provider Inventory** (expandable list of raw providers with health/capacity indicators)  
    - **Metrics** (measured vs. unknown capacity/health, recent failures)  
- `utils/api.js`  
  - API calls for fetching agent/provider data (e.g., `/api/v1/agents/{id}/details`).  
- `styles/dashboard.css`  
  - Styling for health indicators (green/red/yellow), expandable sections.  

**Backend (src/api):**  
- `routes/agent_routes.py`  
  - Endpoints:  
    - `GET /agents/{agent_id}` (returns agent + provider chain data)  
    - `GET /providers` (raw provider inventory, filtered by agent ID).  
- `models/agent.py`  
  - Database schema for logical agents (stable identity, role, fallback chain).  
- `models/provider.py`  
  - Provider inventory (capacity, health, authentication).  

**Tests (tests):**  
- `test_agent_dashboard.py`  
  - Mocks backend responses to verify UI displays correct agent/provider data.  
- `test_api_routes.py`  
  - Validates API returns measured vs. unknown capacity/health.  

**Docs (docs):**  
- `user_guide.md`  
  - Instructions for using the unified view (model rotation, provider inventory).  

---

### **Sequence**  
1. **Backend**:  
   - Extend `agent.py` to include fallback chain, capacity, health fields.  
   - Create `/agents/{id}/details` endpoint to merge agent + provider data.  
2. **Frontend**:  
   - Build `AgentModelDashboard.jsx` with tabs and expandable sections.  
   - Integrate with API to display agent identity, provider inventory, and metrics.  
3. **Testing**:  
   - Write unit tests for API routes (validate data structure).  
   - Write E2E tests for dashboard (verify UI reflects backend state).  

---

### **Assumptions**  
- FG-020 has already established agent/provider data models and authentication.  
- Provider administration controls (e.g., edit/delete) remain in existing routes.  
- "Measured vs. unknown" capacity/health is derived from backend metrics (e.g., `capacity_status` enum).  

---

### **Risks**  
- Overlapping with FG-020’s provider inventory logic (ensure no duplication).  
- UI complexity may require additional state management (e.g., Redux for agent/provider data).  

---

### **Handoff**  
- **To**: Frontend/backend teams + QA.  
- **Deliverables**:  
  - Working dashboard component with backend integration.  
  - Test coverage for acceptance criteria (model rotation, health/capacity accuracy).  
  - Documentation for provider admin controls (ensure they remain accessible).  

---

### **Completion Artifacts**  
- `diffs/FG-060_frontend.patch` (UI changes).  
- `diffs/FG-060_backend.patch` (API/models).  
- `test_results/FG-060_e2e.html` (dashboard validation).  
- `README.md` updates for user guide.  

--- 

This scaffold assumes no destructive changes (per safety rules) and focuses on read-only views with feature flags enabled.
