# Qwen Mini Scaffold: FG-170

- Generated: `2026-08-04T16:08:17.548297+00:00`
- Model: `local-qwen3-14b-debian`

```json
{
  "scaffold": {
    "files": [
      "api/delete_api.py", 
      "services/delete_service.py", 
      "models/artifact.py",
      "frontend/components/DeleteConfirmModal.jsx",
      "frontend/state/delete_slice.js",
      "tests/test_delete_api.py",
      "migrations/0017_add_trash_field.py"
    ],
    "components": [
      "TrashPreviewModal (frontend)",
      "DeleteConfirmationForm (frontend)",
      "ArtifactTrashService (backend)",
      "AuditLogger (backend)"
    ],
    "sequence": [
      "User selects items via filter",
      "API requests preview (GET /delete-preview)",
      "Backend validates permissions and returns count/size",
      "User confirms with typed token and CSRF check",
      "Backend trashes items (soft delete with retention)",
      "Audit event logged with batch metadata"
    ],
    "assumptions": [
      "FG-140/FG-150 provide trash retention infrastructure",
      "Database supports soft deletes with trashed_at timestamp",
      "User authentication/authorization is already enforced"
    ],
    "risks": [
      "Large batch sizes may cause memory issues (mitigated by max_batch_size=1000)",
      "Race conditions during concurrent deletions (mitigated with idempotency tokens)"
    ],
    "handoff": {
      "backend": "Implement API endpoints and service logic with audit logging",
      "frontend": "Build confirmation UI with preview display and token input",
      "qa": "Validate edge cases: pinned/assets, filter changes during preview"
    }
  }
}
```
