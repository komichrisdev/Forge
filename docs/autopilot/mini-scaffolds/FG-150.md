# Qwen Mini Scaffold: FG-150

- Generated: `2026-08-04T14:24:58.600677+00:00`
- Model: `local-qwen3-14b-debian`

```json
{
  "scaffold": {
    "files": [
      "src/services/archive_generator.py",          // Core ZIP creation logic with streaming
      "src/manifest_builder.py",                   // Manifest/checksum generation
      "src/limiter.py",                            // Enforces count/byte/time limits
      "src/temp_storage.py",                       // Manages partial archives
      "src/cleanup_task.py",                       // Orphaned data removal
      "tests/test_archive_generation.py",          // Fixture/edge case tests
      "docs/FG-150_architecture.md",               // System overview
      "migrations/0015_archive_limits.py"          // DB schema for tracking archives
    ],
    "sequence": [
      "Request validation -> Snapshot resolution -> Limit enforcement -> ZIP streaming -> Manifest generation -> Progress tracking -> Cleanup",
      "Temp storage uses atomic writes with unique IDs",
      "Manifest includes SHA-256 checksums and file metadata"
    ],
    "assumptions": [
      "Existing auth system provides artifact permissions",
      "Filesystem has 10GB+ tmp space for large archives",
      "ZIP library supports streaming (e.g., zipfile.ZipFile with 'w')",
      "Collection snapshot is immutable during generation"
    ],
    "risks": [
      "Memory spikes during large archive creation",
      "Race conditions in temp storage cleanup",
      "Manifest generation lag causing incomplete ZIPs",
      "File path normalization vulnerabilities"
    ],
    "handoff": {
      "code_review": "Focus on limit enforcement and temp storage patterns",
      "testing": "Validate 100% of acceptance criteria with 500MB+ fixture",
      "docs": "Update API docs for new /v1/archive endpoint",
      "monitoring": "Add metrics for archive size/timeout failures"
    }
  }
}
```
