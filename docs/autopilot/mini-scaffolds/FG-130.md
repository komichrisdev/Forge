# Qwen Mini Scaffold: FG-130

- Generated: `2026-08-04T12:36:52.659576+00:00`
- Model: `local-qwen3-14b-debian`

{
  "files": {
    "models": ["Batch", "Job", "Collection"],
    "routers": ["swarm_router.py", "batch_router.py"],
    "services": ["job_executor.py", "state_manager.py"],
    "tests": ["test_batch_sequence.py", "test_recovery.py", "test_concurrency.py"],
    "config": ["gpu_quotas.yaml", "idempotency_keys.yaml"],
    "migrations": ["add_batch_relations.py", "add_job_states.py"],
    "frontend": ["dashboard/components/JobQueue.jsx", "dashboard/components/CollectionViewer.jsx"]
  },
  "sequence": [
    "User creates parent batch with ordered prompts",
    "System generates child jobs with idempotency keys",
    "Swarm router queues jobs in sequence with GPU quota checks",
    "Job executor processes one job at a time, updating batch state",
    "Pause/resume/cancel actions update job states in DB",
    "On restart, state manager resumes from last completed job",
    "Artifacts stored in virtual collection with batch ID"
  ],
  "assumptions": [
    "GPU quota enforcement exists in existing infrastructure",
    "Virtual collections are pre-configured in storage layer",
    "Database supports transactional updates for batch/job states"
  ],
  "risks": [
    "Race conditions during job state transitions",
    "Incomplete recovery from partial failures",
    "Idempotency key collisions with concurrent batch creation"
  ],
  "handoff": {
    "review": ["Implementation diff with batch/job state transitions", "Test coverage for 5-item fixture scenarios"],
    "docs": ["Updated API docs for batch operations", "Troubleshooting guide for job recovery"],
    "monitoring": ["Add metrics for queued jobs, batch completion rates, and recovery attempts"]
  }
}
