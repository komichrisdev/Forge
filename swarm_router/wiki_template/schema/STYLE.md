# Style

Use concise Markdown, stable headings, plain language, and explicit source
tokens beside material claims. Separate installed fact, approved decision,
proposal, conflict, and unknown. Avoid hidden assumptions, raw credentials,
private paths, copied conversation text, and generated filler.

Titles and aliases may use Unicode. IDs, project names, tags, source IDs and
Jira keys remain normalized ASCII identifiers.
